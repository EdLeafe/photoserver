Change the image storage to a cloud object store.
Allow for batch processing of images:
    copy a bunch to a folder
    for each:
        create db entry
        create thumb
        upload to cloud



